import styled from "styled-components";
import { background2, spacing } from "../../constants/styles";

export const Container = styled.div`
  display: flex;
  flex-direction: row-reverse;
  padding: ${spacing(0.5)}px;

  background: ${background2};
`;
