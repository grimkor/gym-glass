import styled from "styled-components";
import { background2, background3, spacing } from "../../constants/styles";

export const Container = styled.a`
  height: ${spacing(8)}px;
  background: ${background3};
  border-bottom: 1px solid ${background2};
`;
